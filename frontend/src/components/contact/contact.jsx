import { Box, Typography } from "@mui/material";

export default function Contact() {
    return(
        <Typography sx={{fontSize:'50px', alignItems:'center', display: 'flex', color:'white'}}>
           ContactPage 
        </Typography>
    )
}